/**
 * Provides tools and utils for webDriver crawling or filling any data using
 * automated browser. It's addition to official Selenium and it's webDriver.
 * <p>
 * The central functional class is {@link el.selenium.drivers.page.PageDriver} which wraps real
 * instance of WebDriver.
 */
package el.selenium;
